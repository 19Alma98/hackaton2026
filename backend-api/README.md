# Backend API

Eventuale API REST (Python/FastAPI o Flask) per il frontend: lettura dati dalla chain, configurazione (endpoint RPC, indirizzi contratti).

Opzionale: il frontend può parlare direttamente con un nodo via Web3/RPC.
